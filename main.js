document.addEventListener("DOMContentLoaded",()=>{const toggle=document.querySelector(".nav-toggle"),nav=document.querySelector(".nav");if(toggle)toggle.addEventListener("click",()=>nav.classList.toggle("open"));const obs=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)e.target.classList.add("visible")}),{threshold:.12});document.querySelectorAll(".reveal").forEach(el=>obs.observe(el));document.querySelectorAll("[data-count]").forEach(el=>{const target=+el.dataset.count;let n=0;const tick=()=>{n=Math.min(target,n+Math.max(1,Math.ceil(target/30)));el.textContent=n;if(n<target)requestAnimationFrame(tick)};new IntersectionObserver(es=>{if(es[0].isIntersecting){tick();es[0].target.closest(".stats")?.querySelectorAll("[data-count]").forEach(x=>x.dataset.done="1")}}, {threshold:.5}).observe(el)})});